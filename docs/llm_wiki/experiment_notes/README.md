# Experiment Notes

Use this folder only after actual model or composite-evaluation runs begin.

Suggested folders:

```text
time_series/
vlm/
composite_eval/
vision_optional/
artifacts/
```

Every note must link to its command, output artifact, sample size, split, and
limitations.
